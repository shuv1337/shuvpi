save-exact=true
min-release-age=2
# npm 12 blocks tarball-URL dependencies by default. `root` permits only the URLs
# this repo declares itself (packages/web-ui depends on the sheetjs xlsx tarball),
# not arbitrary URLs pulled in transitively.
allow-remote=root
